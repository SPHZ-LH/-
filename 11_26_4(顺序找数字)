#include<stdio.h>
int main(void)
{
	int arr[] = { 1,2,3,4,5,6,7,8,9 };
	int k = 7;
	int i = 0;
	int num = sizeof(arr) / sizeof(arr[0]);
	for (i = 0; i < num; i++)
	{
		if (k == arr[i])
		{
			printf("找到数字：%d\n", i);
			break;
		}
	}
	if (i == num)
		printf("找不到\n");
	return 0;
}
